

const storeName = "detailInformation"; // Tên store trong IndexedDB
const thunkDB = "idb/"
const thunkName = "DetailInformation";

const thunkAction = {
  fetch: "fetch",
  set: "set",
  delete: "delete",
}

// const idb = new IDBManager<List>(storeName);